services:
  - type: web
    name: stock-portfolio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_server_fixed:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
